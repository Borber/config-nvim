# 下一步优化计划

- [x] 抽取通用 Telescope 选择器外壳, 先复用到 Overseer 和 Toggleterm 的 picker, 保留各自业务动作不变
- [x] 梳理 mini.sessions 的职责边界, 将 session 文件解析、buffer 过滤、Neogit 刷新拆成更小的内部 helper
- [x] 继续收口 path / buffer helper 的调用点, 确认没有重复判定重新散落到插件模块里
- [x] 为 starter、recent paths、mini.files、session restore 补一组最小 headless 行为验证, 防止后续简化误改交互边界

# 优化Overseer的启动

- [ ] 任务启动时, 无须默认展开底部任务详情, 只在用户主动展开时才加载详情内容, 或者是, 任务失败时, 自动展开详情以便用户查看错误信息
